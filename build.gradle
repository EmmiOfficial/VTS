buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.3.0' // Upgraded AGP for better compatibility
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
